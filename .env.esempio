# copiare in .env (che non va versionato)

MONGO_URL=mongodb://localhost:27017/aulalibera

# in produzione va generato casualmente
JWT_SEGRETO=sviluppo-non-usare-in-produzione

JWT_SCADENZA=8h

PORTA=3000

# gli orari di apertura/chiusura sono in ora locale: deve essere il fuso dell'ateneo
TZ=Europe/Rome
